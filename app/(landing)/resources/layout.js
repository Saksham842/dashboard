import '../../../src/landing/styles/landing.css';

export const metadata = {
  title: 'Resources · intervieHire',
  description: 'Explore FAQs, founder insights, and expert articles from the intervieHire team.',
};

export default function ResourcesLayout({ children }) {
  return <>{children}</>;
}
